{{- if eq .chezmoi.os "darwin" -}}
#!/usr/bin/env bash

set -ex pipefail

{{ $home := .chezmoi.homeDir }}

{{- range $font := list "FiraCode.zip" "FiraCodeMono.zip" "Meslo.zip" "Hack.zip" -}}

{{ $file := joinPath $home "Library/Fonts" $font }}
# Hash: {{ include $file | sha256sum }}
unzip $file -d {{ joinPath $home "Library/Fonts" }}

{{- end }}

{{- end }}
